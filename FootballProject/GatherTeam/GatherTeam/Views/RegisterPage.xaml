<Page
    x:Class="GatherTeam.RegisterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GatherTeam"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <TextBox PlaceholderText="Login" HorizontalAlignment="Left" Margin="94,59,0,0" TextWrapping="Wrap" Text="{Binding Path=Login}" VerticalAlignment="Top" Width="243"/>
        <TextBox PlaceholderText="Email" HorizontalAlignment="Left" Margin="94,125,0,0" TextWrapping="Wrap" Text="{Binding Path=Email}" VerticalAlignment="Top" Width="243"/>
        <PasswordBox PlaceholderText="Password" Password="{Binding Password}" HorizontalAlignment="Left" Margin="94,259,0,0" VerticalAlignment="Top" Width="243"/>
        <PasswordBox PlaceholderText="Confirm password" Password="{Binding ConfirmPasswod}" HorizontalAlignment="Left" Margin="94,315,0,0" VerticalAlignment="Top" Width="243"/>
        <TextBox PlaceholderText="Phone" HorizontalAlignment="Left" Margin="94,191,0,0" TextWrapping="Wrap" Text="{Binding Path=Phone}" VerticalAlignment="Top" Width="243"/>
        <Button Content="Sign in" HorizontalAlignment="Left" Margin="94,419,0,0" VerticalAlignment="Top" Height="137" Width="231" />
    </Grid>
</Page>
